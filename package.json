{
  "name": "katgrapher-studio",
  "version": "1.0.0",
  "description": "Portfolio website for katgrapher.studio",
  "main": "index.html",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && tailwindcss -i ./assets/input.css -o ./dist/output.css --minify && cp index.html dist/index.html && cp gallery.html dist/gallery.html && cp -R scripts dist/scripts"
  },
  "keywords": [
    "portfolio",
    "photography",
    "sanity-cms"
  ],
  "author": "KatGrapher",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}